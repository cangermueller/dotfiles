import rlcompleter
import pdb
pdb.Pdb.complete = rlcompleter.Completer(locals()).complete

_globals = globals()
_ = tf.logging.set_verbosity(tf.logging.ERROR) if 'tf' in _globals else None
_ = logging.set_verbosity(logging.ERROR) if 'logging' in _globals else None
